(defcfg
    process-unmapped-keys yes
    concurrent-tap-hold yes
    windows-altgr cancel-lctl-press
)

(defsrc
    caps esc
)

(deflayer default
    @escctrl @esc-behavior
)

(defalias
    escctrl (tap-hold 0 190 esc lctrl)
    esc-behavior (tap-hold 190 190 esc caps)
)

;; vim: syntax=lisp
